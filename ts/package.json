{
  "name": "mathviz-renderer",
  "version": "0.1.0",
  "description": "Three.js renderer for mathviz scene documents",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "three": "^0.170.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "puppeteer": "^23.0.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0"
  }
}
